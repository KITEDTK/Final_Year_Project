BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[ClothDetails] DROP CONSTRAINT [ClothDetails_amount_df];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
